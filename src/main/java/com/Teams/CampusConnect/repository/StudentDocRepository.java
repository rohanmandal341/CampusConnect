package com.Teams.CampusConnect.repository;

import com.Teams.CampusConnect.model.StudentDoc;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StudentDocRepository extends JpaRepository<StudentDoc,Long> {
}
